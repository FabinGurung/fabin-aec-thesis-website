# No application runtime variables are required.
# Optional local process/test overrides; keep every value blank in portable source.
PORT=
HOST=
BASE_URL=

# Set automatically by GitHub Actions. Leave blank for local development.
NEXT_PUBLIC_BASE_PATH=
